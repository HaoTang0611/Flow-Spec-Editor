# CommunicationProtocol

**定位：語言無關 × 專案無關**

本文件定義通訊協定類型枚舉，用於 `CommunicationOptions.ProtocolType` 欄位。

---

## 枚舉值

| 值 | 說明 |
|----|------|
| `Serial` | 序列埠通訊，對應具體配置型別為 `SerialCommunicationOptions` |
| `Tcp` | TCP 通訊，對應具體配置型別為 `TcpCommunicationOptions` |
